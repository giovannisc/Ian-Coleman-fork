{
  "name": "bip39-modernization",
  "private": true,
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "preview": "npm run preview -w web",
    "lint": "npm run lint -w web",
    "test": "npm run test -w web",
    "deploy": "npm run build && npx sst deploy",
    "dev:sst": "npx sst dev -- npm run dev -w web"
  }
}
